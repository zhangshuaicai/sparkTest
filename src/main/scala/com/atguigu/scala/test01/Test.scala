package com.atguigu.scala.test01

object Test {
    def main(args: Array[String]): Unit = {
        val tuples: List[(Int, Int, Int)] = List((1,1,2),(2,2,3))
//        val flatten = tuples.flatten()
//        println(flatten)
    }
}
